/**
 * Reader app composition root. Import `bootstrapReader` from `./bootstrap` or use `../main` entry.
 */
export { bootstrapReader } from "./bootstrap";
export { dismissSplashWhenReady } from "./splash";
