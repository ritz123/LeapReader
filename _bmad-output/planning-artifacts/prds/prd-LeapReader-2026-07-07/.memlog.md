---
topic: LeapReader LLM Integration PRD
updated: 2026-07-07T20:52
---

- (decision) LLM features are Electron-desktop-only; Android and web builds unchanged (A1)
- (decision) Python FastAPI backend bundled inside Electron, spawned as child process, not user-managed (A4)
- (decision) Default model: llama3.2:2b-instruct-q4_K_M via Ollama; user installs Ollama separately (A8, A9)
- (decision) Web search = DuckDuckGo/SearXNG results injected as prompt context, not live browsing (A3)
- (decision) AI Panel is a bottom drawer; keeps existing split-pane layout intact (A5)
- (decision) Chat history is session-only, not persisted across restarts in v1 (A7)
- (event) PRD draft written; pending user review of assumptions and open questions
- (decision) Default model updated to llama3.2:3b Q4_K_M based on research; fallbacks qwen3.5:2b and gemma3:1b
- (decision) Web search default: duckduckgo-search Python library for v1; SearXNG as advanced option
- (override) A5 revised: two per-pane chat panels (left bound to left doc, right bound to right doc), within pane boundaries
- (override) A1 revised: all AI changes on dedicated Electron feature branch; Android/web builds untouched
- (override) A6 revised: each chat permanently bound to its own pane document; no active-focus switching
- (override) A8 revised: Ollama fully managed (auto-download binary + auto-start); user installs nothing manually
- (override) A9 revised: Model Browser allows user to pull/switch any Ollama model, not just default
- (decision) OQ-1 resolved: duckduckgo-search confirmed as v1 web search backend; SearXNG deferred to v2
- (decision) New FR-19: Review automatically extracts citations and looks them up via web search; results included in review output
- (override) FR-8/FR-19 redesigned: Review uses ReAct agent loop (Ollama tool-calling) from manuscript2presentation pattern; model autonomously calls web_search+fetch_url
- (decision) Review backend uses pypdf for extraction (not pdfjs frontend layer); 50k char limit matching existing project
- (decision) Review system prompt based on ai-dm-paper-review SKILL.md (7 sections, citation verification protocol)
- (decision) Python deps confirmed: ollama>=0.4, pypdf>=4, duckduckgo-search, requests, fastapi, uvicorn
- (decision) A11: file path sent to backend for Review (pypdf), pdfjs text sent for chat/summarize
- (decision) A12: Review offline mode ships v1 as secondary action on Review button
- (decision) ai-dm-paper-review SKILL.md bundled as asset; gemma3:1b flagged as no-tool-calling fallback
- (event) PRD finalized: status set to final; polish pass complete; 18 FRs, 4 UJs, 12 assumptions, 7 OQs
- (event) Rubric review findings addressed: OQ-2 resolved (uv venv), NFR-11 corrected (150MB), FR-19 added (Review offline), SM-2/UJ-1 reconciled, FR-8 7-section structure clarified, A13 indexed, UJ-4 protagonist renamed. PRD is final.
- (event) Reconciliation fixes applied: UJ-2 section list corrected (all 7+8 subsections), Confidence Level added to FR-8, SSE event types named in FR-9, FR-8 streaming adaptation note added, PAGE BREAK noted, FR-18 pane-origin NOTE FOR PM added, UI layout conflict notes added in 4.1. PRD truly final.
